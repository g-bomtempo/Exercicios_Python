# Exercício Python 50: Desenvolva um programa que leia seis números inteiros e mostre a
# soma apenas daqueles que forem pares. Se o valor digitado for ímpar, desconsidere-o.

soma = 0
contador = 0

for c in range(1, 7):
    numero = int(input('Entre o número {} da sequencia: '.format(c)))
    if numero % 2 == 0:
        soma = soma + numero
        contador = contador + 1
print('Dos 6 números informados, dos {} informados, a soma dos números pares é {}'.format(contador, soma))
