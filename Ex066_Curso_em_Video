# Exercício Python 66: Crie um programa que leia números inteiros pelo teclado.
# O programa só vai parar quando o usuário digitar o valor 999,
# que é a condição de parada. No final, mostre quantos números foram digitados e
# qual foi a soma entre elas (desconsiderando o flag).

contador = soma = 0

while True:
    entrada = int(input('Entre o número desejado [999 para parar o programa]: '))
    if entrada == 999:
        break
    else:
        soma = soma + entrada
        contador = contador + 1

print(f'Tivemos {contador} número adicionados e a sua soma é {soma}')
